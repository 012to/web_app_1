<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-12">
        <div class="card-body">
          <h2 class="card-title text-center" ref="/users/<%= current_user.id %>">マイページ</h2>
            <%= render partial: 'shared/posts', locals: { posts: @posts } %>
        </div>
        <a href="<%= edit_user_registration_path(current_user) %> class="btn btn-primary"">登録情報の編集</a>
      </div>
    </div>
  </div>
